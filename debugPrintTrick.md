# trick learned from work



      func dLog(_ message: String) {
          #if DEBUG
              print(message)
          #endif
      }


//so only print when is debugging

# In debugger(po) p *object, will show the thing in the object
